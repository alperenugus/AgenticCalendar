{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Per-service config for the frontend. Without this, the service inherits the backend's root railway.json (DOCKERFILE builder + /actuator/health check), which breaks the frontend's git-based deploys. Nixpacks builds the Vite SPA and runs the package.json start script.",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
