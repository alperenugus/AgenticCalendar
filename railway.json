{
  "$schema": "https://railway.app/railway.schema.json",
  "$comment": "Railway will use Dockerfile - no start command needed (Dockerfile CMD handles it)",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
